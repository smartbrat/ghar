# Ghar.tv component index

> Generated by `npm run build:components`. Do not edit. Browse at `/components`.
> Pick a component here, then read ONLY its source file. Full data: `components.json`.

137 components: 120 paste, 13 include, 4 call. 50 still carry inline CSS.

**Kinds.** `paste`: copy the markup. `include`: never paste; add the PARTIAL marker and run `npm run build:partials`. `call`: load the script and call the function.

## Foundations

| id | kind | what it is | source |
|---|---|---|---|
| `type-section` | paste | The three text roles every section header is built from: a tracked-caps Inter eyebrow, a Gazpacho Bold display title with size steps, and an Inter lead paragraph. The head layouts arrange them; these classes paint them. | `_dev/reference/components/type-section.html` |

## Buttons & links

| id | kind | what it is | source |
|---|---|---|---|
| `button` | paste | The general-purpose .btn chassis for page actions that are not the section CTA: load more, a strip's view-all, a contribute panel's two actions. One base class for geometry, one modifier for the colour role. | `_dev/reference/components/button.html` |
| `button-card-link` | paste | The text plus arrow cue that sits inside a card: no border, no fill, 13px ink. On hover the gap widens and the arrow slides; the colour never changes. | `_dev/reference/components/button-card-link.html` |
| `link-inline` | paste | Two small text-link atoms for meta rows and quiet footers of a block: a faint muted link that brightens to ink, and the red arrow link that is one of the few sanctioned red text uses. | `_dev/reference/components/link-inline.html` |
| `button-pair` | paste | The canonical treatment whenever two buttons sit side by side: the main action dark, the secondary a quiet white that only warms on hover. The hierarchy survives the hover state. | `_dev/reference/components/button-pair.html` |
| `button-section-cta` | paste | The one outlined button in a section header that sends the reader to the full vertical. Ink outline, fills with ink on hover, arrow nudges right. | `_dev/reference/components/button-section-cta.html` |

## Layout

| id | kind | what it is | source |
|---|---|---|---|
| `grid-3col-editorial` | paste | A supporting left column, a prominent centre column and a supporting right column. On phones the columns stack; between 576px and 1079px the centre spans the top with the sides paired below; from 1080px it is three columns with hairline dividers around the centre. | `_dev/reference/components/grid-3col-editorial.html` |
| `panel-bleed` | paste | A contained, rounded content panel that becomes a full-width band below 1024px, so its content lands on the same gutter as every other section instead of being indented twice on a phone. | `_dev/reference/components/panel-bleed.html` |
| `section-head` | paste | Eyebrow and display title on the left, a text plus arrow See all link on the right, baseline-aligned in one flex row. The compact variant turns the title into a small tracked-caps label for sub-clusters. | `_dev/reference/components/section-head.html` |
| `section-head-split` | paste | The storytelling section opener: eyebrow on its own row, then title on the left and lead plus the section CTA stacked on the right from 1024px. Below that everything stacks. Every homepage section reads with the same rhythm. | `_dev/reference/components/section-head-split.html` |
| `section-wrap` | paste | The outer shell of every section. The <section> itself gets its vertical padding from one portal-wide rule; .section-block carries an optional tint band; .section-wrap centres the content at --max-w with the responsive --pad-h gutter. | `_dev/reference/components/section-wrap.html` |

## Primitives

| id | kind | what it is | source |
|---|---|---|---|
| `accordion` | paste | Native details and summary rows between hairlines, for FAQs and long specs. Works before any script loads, the whole row is the hit area, and the plus turns into a minus by rotating one stroke away. | `_dev/reference/components/accordion.html` |
| `breadcrumbs` | paste (inline CSS) | The inner-page trail above a search or listing head: muted links, a small faded chevron between them, and the current page in bold ink with no link. | `_dev/reference/components/breadcrumbs.html` |
| `load-more` | paste (inline CSS) | A single button under a directory grid that reveals the next batch in place. The shared gharGridReveal helper counts whole rows off the live column count, so no batch ever leaves a ragged part-row, and it hides the button when nothing is left. | `_dev/reference/components/load-more.html` |
| `pagination` | paste | Numbered pages for a result set someone will come back to: prev and next, page numbers with a gap marker, and a count that states the total. On phones the numbers collapse to the current page. | `_dev/reference/components/pagination.html` |
| `segmented-control` | paste | A compact pill group for two to five mutually exclusive options. As tabs it swaps a panel in place; as .seg--choice it answers an either/or inside a form with a sliding thumb. One delegated listener in main.js drives both, including arrow keys and roving tabindex. | `_dev/reference/components/segmented-control.html` |
| `skeleton` | paste | Grey placeholder blocks with a soft sweep, shaped like the content about to arrive. The sweep stops under reduced motion, and the class hides any text already inside the element. | `_dev/reference/components/skeleton.html` |
| `skip-link` | paste | The first focusable element on the page. Invisible until a keyboard user tabs to it, then it pops to the top-left as an ink pill that jumps straight past the navbar to the main content. | `_dev/reference/components/skip-link.html` |
| `tooltip` | paste | A CSS-only bubble that shows on hover and on keyboard focus. Either a small ? trigger glosses a term, or .tip__anchor names a control that has no room for its own label, such as an icon button in a bar. | `_dev/reference/components/tooltip.html` |
| `tabs` | paste | A wider row of tabs inside a page body. The selected tab's underline wipes in from the left, and the same delegated listener as the segmented control swaps the panels and handles arrow keys. | `_dev/reference/components/tabs.html` |
| `utilities` | paste | Single-purpose helper classes for the jobs every card and section repeats: fill a box, crop an image, lock an aspect ratio, clamp or truncate backend text, align numbers, collapse empty fields, and set a brand colour directly on an element. | `_dev/reference/components/utilities.html` |

## Cards

| id | kind | what it is | source |
|---|---|---|---|
| `card-hover-lift` | paste | The standard hover for a bordered whole-card link: a 3px rise, a soft layered shadow and a slightly darker border. One co-class on the card root. | `_dev/reference/components/card-hover-lift.html` |
| `card-hr` | paste (inline CSS) | The design-pillar story card in three shapes: a 4:5 overlay card with the title over a soft scrim and a dek that opens on hover, an editorial card with a 4:3 image above the text, and a 16:9 lead card that can carry a sponsored strip. | `_dev/reference/components/card-hr.html` |
| `feat-list` | paste | A short what's-included list inside a card: a hairline rule on top, then rows of a 22px line icon beside a bold title and a one-line description. | `_dev/reference/components/feat-list.html` |
| `thumb-play` | paste | A 56px frosted dark play circle centred over a video thumbnail. The wrapper covers the thumbnail and ignores the pointer, so the whole card stays the link. | `_dev/reference/components/thumb-play.html` |

## Carousels

| id | kind | what it is | source |
|---|---|---|---|
| `brand-ticker` | paste (inline CSS) | A continuous-drift marquee of brand wordmarks or logos with a one-line description under each, divided by short hairlines. It drifts on its own, can be dragged with momentum, and fades softly at both edges. | `_dev/reference/components/brand-ticker.html` |
| `carousel-chips` | paste | A bleed-edge row of icon chips, such as Browse by city, on the shared rail chassis. Drags on desktop with hover arrows and soft edge fades, and switches to native finger scroll on touch. | `_dev/reference/components/carousel-chips.html` |
| `carousel-init` | call | The one JS function behind every horizontal rail on the portal. Give it the rail-outer and rail, plus whichever arrows, fades and paginator buttons the markup has; it handles drag, snap, arrows, edge state, autoplay in view and native finger scroll on touch. | `_dev/reference/components/carousel-init.html` |
| `carousel-cards-paged` | paste | A row of substantial cards, such as Featured projects, that pages with a centred prev and next paginator below the rail. JS sizes each card from a fractional --n curve, so the next card peeks on in-between widths and one card centres on phones. | `_dev/reference/components/carousel-cards-paged.html` |
| `carousel-tag-strip` | paste | The row of category pills between a section head and the rail below it. Sits still when every pill fits; only when it overflows does it bleed, fade at the edges, drag and show hover arrows. | `_dev/reference/components/carousel-tag-strip.html` |

## Editorial blocks

| id | kind | what it is | source |
|---|---|---|---|
| `in-focus` | paste | The featured slot at the top of a directory. One identity-led block per day, either a brand (logo, name, lead copy, meta and three work tiles) or a person (portrait or monogram, role, quote and where they appear on Ghar.tv), rotated and de-duplicated against the grid below by one shared function. | `_dev/reference/components/in-focus.html` |

## Forms

| id | kind | what it is | source |
|---|---|---|---|
| `form-choice` | paste | The small choice controls of the form system. The native input stays in the DOM and is visually replaced, so keyboard, submission and assistive tech work with no extra code. The whole label is the hit area. | `_dev/reference/components/form-choice.html` |
| `form-either-or` | paste | A two or three answer question (buy or rent, residential, commercial or land) drawn as one segmented switch. Radio semantics on the .seg paint, with a single thumb that slides and resizes to the chosen answer and stacks when the labels cannot fit one row. | `_dev/reference/components/form-either-or.html` |
| `form-select` | paste | A plain select.jm-field that main.min.js upgrades on load into a portal-styled trigger and option panel, with keyboard, type-ahead and flip-up. The native select stays underneath and keeps owning name, value, required and form.reset(). | `_dev/reference/components/form-select.html` |
| `form-label` | paste | A persistent label above a .jm-field, an optional red asterisk for a field that blocks submit, and a muted help line tied to the control. For long multi-section flows where a placeholder would vanish as the reader types. | `_dev/reference/components/form-label.html` |
| `form-field-states` | paste | Error, valid and disabled states for .jm-field, the per-field error line, and the form-level error above submit. gharFieldError moves the red border, the message and aria-invalid together so none is left behind. | `_dev/reference/components/form-field-states.html` |
| `form-file-upload` | paste | A drop zone over a real file input: click to open the picker or drag files onto it. Chosen files list underneath with their size and a remove button, wired by main.min.js through data-file-list. | `_dev/reference/components/form-file-upload.html` |
| `form-field` | paste | The one control class of the portal form system. .jm-field paints text, email, select and textarea identically, .jm-phone pairs a mobile input with the +91 prefix, and .brf-row puts two controls on one line. | `_dev/reference/components/form-field.html` |
| `form-structure` | paste | Sections for a long form: .jm-fieldset groups with an eyebrow legend, a line of intro and a hairline between them, a live character counter under a capped textarea, and the .jm-actions row with the form buttons. | `_dev/reference/components/form-structure.html` |
| `brief-form` | include | The single source of the project brief fields, shared by the brief modal and the two brief pages (/brands/brief and /people/brief). Placeholder-driven .jm-* controls, .brf-row pairs, a hidden form-level .brf-error, the submit and a no-obligation note. | `_dev/reference/components/brief-form.html` |
| `form-chips-tiles` | paste | Large selection controls where the option itself is the target. .jm-chip is a pill for many short options, .jm-tile a block for a few that need explaining. Both are native checkboxes or radios underneath, styled off :has(). | `_dev/reference/components/form-chips-tiles.html` |
| `form-success` | paste | What replaces a form in place once it is sent: a sage tick, a Gazpacho title and a line that says what happens next. The reader who just sent a brief gets a destination, not a message that vanishes. | `_dev/reference/components/form-success.html` |

## Overlays

| id | kind | what it is | source |
|---|---|---|---|
| `contact-modal` | include | Contact a brand: an editorial two-line heading with the brand's name, then name, mobile, optional email and a short note. The brand name travels in a hidden field. Used on the brand directory, brand search, brand profiles and people pages. | `_dev/reference/components/contact-modal.html` |
| `modal-confirm` | paste | A small modal that asks before an action that cannot be undone. A red-tinted mark, a question, the consequence in plain words, and two buttons: the safe choice left, the destructive verb right in the one red fill the form system allows. | `_dev/reference/components/modal-confirm.html` |
| `lightbox` | paste | Full-size image viewing with Fancybox v6.1 from the CDN. Links carrying the same data-fancybox group open as one gallery with prev and next, captions and thumbnails; /gt-fancybox.js layers the portal's tap behaviour on top: a backdrop tap steps back instead of closing, and Work slides cycle caption, cover and fit. | `_dev/reference/components/lightbox.html` |
| `modal-shell` | paste | The one chassis every portal modal rides: .jm-overlay plus .jm-modal at a size token, an optional title row with the close button, and .jm-body as the single scroll container. Toggle .jm-open on both to show it. Below 744px it becomes a full-screen sheet that slides up. | `_dev/reference/components/modal-shell.html` |
| `brief-modal` | include | Share your project brief, in a dialog any page can open. Name, mobile, email, what and where, budget and timeline, a note, then Send brief. Triggers stay real links to /brands/brief, and the shared script opens the modal instead when it is on the page. | `_dev/reference/components/brief-modal.html` |
| `share-sheet` | paste | The portal share sheet as a drop-in: give any element data-share and load /ghar-share.js. The script injects one .jm-modal with a preview card, Copy link, WhatsApp, X, Facebook, LinkedIn and Email tiles, a QR toggle and the native share button, so a page never carries its markup. | `_dev/reference/components/share-sheet.html` |
| `join-modal` | include | The portal sign-in dialog: mobile number with a searchable country code, password with a show toggle, then Forgot Password and Login with OTP routes, plus Post Property and Post Requirement for new users. Phone and password, no OTP cost on a normal sign in. | `_dev/reference/components/join-modal.html` |
| `subscribe-modal` | include | The newsletter sign-up in a dialog: one email field, a quick arithmetic check, then an in-place done line that closes itself. Rides the canonical .jm-overlay and .jm-modal shell and carries its own small script. | `_dev/reference/components/subscribe-modal.html` |

## Feedback

| id | kind | what it is | source |
|---|---|---|---|
| `toast` | call | A short confirmation or error that appears at the edge of the screen. One function creates the host, stacks them, times them out and animates them away. | `_dev/reference/components/toast.html` |

## Navigation

| id | kind | what it is | source |
|---|---|---|---|
| `subnav` | paste | The second-level bar of a vertical: icon-over-label tabs with an ink underline on the active one. Its own row on mobile and tablet; from 1080px up it is lifted into the masthead so the page keeps one bar. | `_dev/reference/components/subnav.html` |

## Shared chrome

| id | kind | what it is | source |
|---|---|---|---|
| `nav-dark` | include | The shared masthead, bottom bar and footer on a near-black canvas. A token swap on body.nav-dark in nav.css, not a second navbar, so the same partial and every control keep working. | `_dev/reference/components/nav-dark.html` |
| `bottom-bar` | include | The fixed four-tab bar below 744px: Ghar, Post Property, Account, Menu. The partial also carries the pre-paint script that merges the section subnav into the masthead on wide screens. | `_dev/reference/components/bottom-bar.html` |
| `oc-menu` | include | The portal's table of contents, sliding in from the right. Prominent links on top, grouped links below, and L2 panels (cities, events, services, tools) that slide over the list, each its own scroll container. | `_dev/reference/components/oc-menu.html` |
| `site-footer` | include | The four-layer portal footer: subscribe card, brand bar, link columns on the carousel chassis, and the legal bottom bar. Byte-identical on every page that carries it. | `_dev/reference/components/site-footer.html` |
| `site-nav` | include | The one portal masthead. The same partial renders every nav the portal has; body classes pick which pieces show: the search pill, the section lockup, Subscribe or Share a brief, and an in-bar search button. | `_dev/reference/components/site-nav.html` |
| `subscribe-block` | include | The in-page email capture for an editorial stream: one title, one dek, one email field, one button, then a quick sum check and a confirmation. The partial carries its own three-step script. | `_dev/reference/components/subscribe-block.html` |

## Search

| id | kind | what it is | source |
|---|---|---|---|
| `city-match` | call | The one city filter for every directory surface. Given the location text already on a card and the picked city, it says whether the card belongs: a metro matches its satellites and old names, and a multi-city entry matches every city. | `_dev/reference/components/city-match.html` |
| `nav-search-bar` | include | The property search as it sits in the masthead: one pill holding Where, Buy or rent, type and a red Go. The chosen city pins outside a scrolling chip strip, and past the fold the bar collapses into a compact pill. Narrower widths swap to single search-trigger pills that open the search modal. | `_dev/reference/components/nav-search-bar.html` |
| `mobile-search-modal` | include | The universal property search as a stepper: Where, then Buy or rent, then property type, with a Clear all and Search footer. Full-screen white below 744px; from 744px the same node becomes a centred 720px card on a dimmed backdrop. | `_dev/reference/components/mobile-search-modal.html` |

## Motion

| id | kind | what it is | source |
|---|---|---|---|
| `scroll-reveal` | call | Fades content in once as it scrolls into view. The page lists selectors and a primitive per selector; the script adds the classes, an IntersectionObserver adds .is-revealed once, and CSS runs the transition. | `_dev/reference/components/scroll-reveal.html` |

## Media

| id | kind | what it is | source |
|---|---|---|---|
| `film-block` | paste | A centred 16:9 poster for a brand's film that plays in place on click. It scales up from .72 as it scrolls into view when GSAP is on the page; the same script also plays video rail cards in place and drives the hero's 'Watch the Film' cue. | `_dev/reference/components/film-block.html` |
| `photo-lightbox` | paste (inline CSS) | Opens an article photo full-size with a caption, and steps through every photo in the same figure. Self-hosted PhotoSwipe; each image is a plain link to the high-res file, so it still works with JS off. | `_dev/reference/components/photo-lightbox.html` |
| `video-facade` | paste | A still with a play button that becomes a real YouTube player only when clicked, plus optional chapter rows that seek it. No iframe loads until the reader asks for the video. | `_dev/reference/components/video-facade.html` |

## Brand profile

| id | kind | what it is | source |
|---|---|---|---|
| `brand-profile-contact` | paste | The closing card on every brand profile: title, a short lead, the Contact button that opens the shared contact modal, optional reach rows, and info blocks for office and response time. Every colour is a --contact-* token, so a tenant themes it light or dark without touching a rule. | `_dev/reference/components/brand-profile-contact.html` |
| `brand-profile-hero-graphic` | paste (inline CSS) | The opening of a brand profile for a brand with no primary visual: a light split hero with the copy column on the left, a quiet ambient line graphic on the right, and a full-width strip below holding the metrics card and the facts and socials row. | `_dev/reference/components/brand-profile-hero-graphic.html` |
| `brand-profile-hero-photo` | paste (inline CSS) | The full-height opening of an image-led brand profile: a real brand photograph under a dark scrim, the logo tile, tier badge, eyebrow, brand name, one-line promise and a quiet meta row of facts and hover-expand social pills. On phones the photo sits above a light card that holds the copy, with the logo tile straddling the seam. | `_dev/reference/components/brand-profile-hero-photo.html` |
| `brand-profile-about` | paste (inline CSS) | The About chapter of a developer or product brand profile: a Story eyebrow, the brand's promise as a large pull quote, a strip of track-record metrics, then the story prose with a Continue reading collapse beside an In brief colophon of numbered receipts (segments, markets, recognition). | `_dev/reference/components/brand-profile-about.html` |
| `brand-profile-team` | paste (inline CSS) | The Team section of a brand profile: a titled rail of person cards, each with a 4:3 portrait or a monogram tile in the tenant colour, name, role, city, expertise tags and a Connect button. Every card links to that person's /people profile. | `_dev/reference/components/brand-profile-team.html` |
| `brand-profile-tokens` | paste | The small set of CSS custom properties and body attributes that theme a brand profile for one tenant: the brand accent and its soft wash, a dark drawing ink, the neutral ink scale, and the page switches for format and dark theme. Everything visual that differs between tenants should come from these. | `_dev/reference/components/brand-profile-tokens.html` |
| `brand-profile-work` | paste (inline CSS) | The projects or gallery section of a brand profile: a rail of image tiles with a locality eyebrow and project title. Each tile opens a Fancybox lightbox whose side panel (caption below on phones) shows an index, the eyebrow, the title and the project write-up taken from the tile's hidden rich text. | `_dev/reference/components/brand-profile-work.html` |
| `profile-microfooter` | paste | The quiet sign-off at the bottom of every brand and person profile: the Ghar.tv G-mark, a Presented on Ghar.tv caption and three small links, stacked and centred. It replaces the portal footer on microsites. | `_dev/reference/components/profile-microfooter.html` |
| `profile-sticky-contact` | paste | The bar pinned to the bottom of a brand or person profile on phones: a Back disc, a full-width Contact button and a Menu disc that opens the portal off-canvas menu. The markup also carries a Share disc, which shared CSS currently hides. It keeps the one action a visitor came for a thumb away at any scroll depth, and stands down while the on-page contact card is in view. | `_dev/reference/components/profile-sticky-contact.html` |
| `profile-share-sheet` | paste | The Share action on brand and person profiles. Any element with data-brand-share opens one bottom-sheet or centred modal with a preview card, Copy link, WhatsApp, X, Facebook, LinkedIn and Email tiles, a QR toggle and the native share button where the browser has one. | `_dev/reference/components/profile-share-sheet.html` |
| `brand-profile-topbar` | paste (inline CSS) | The single fixed bar on brand and person profiles: Ghar.tv G-mark and a Brands link on the left, and Contact, Share and Menu on the right. It is glass over the hero at rest and turns solid white on scroll, when the G-mark gives way to the tenant name and the Contact pill appears. | `_dev/reference/components/brand-profile-topbar.html` |

## Person profile

| id | kind | what it is | source |
|---|---|---|---|
| `person-profile-contact` | paste | The closing dark card of a /people profile: a Get in touch eyebrow, a two-line Work with / Person Name title, a short reassurance line, an optional list of direct contact links and a primary Get in touch button that opens the contact modal. | `_dev/reference/components/person-profile-contact.html` |
| `person-profile-hero` | paste (inline CSS) | The opening of a /people profile: a portrait (or a monogram tile when no real portrait exists) beside the category badge, name, role, city, a short brief and the ways to reach them, closed by a full-width Specialises in fact bar. | `_dev/reference/components/person-profile-hero.html` |

## Directories

| id | kind | what it is | source |
|---|---|---|---|
| `directory-brand-card` | paste (inline CSS) | The card every brand gets on /brands and on search result grids: a 16:9 brand photo with the logo tile overlapping its lower edge, an optional paid-tier badge, the brand name, locality, up to three category tags and a Contact button. Cards sit in a three, two, one column grid. | `_dev/reference/components/directory-brand-card.html` |
| `directory-results-empty` | paste (inline CSS) | What people and brands search show when the filters leave nothing: a No matches eyebrow, a plain one-line title, a short suggestion and two actions, share a project brief or reset the filters. | `_dev/reference/components/directory-results-empty.html` |
| `directory-results-toolbar` | paste (inline CSS) | The bar above a people or brands search grid: the searched terms restated as the page H1, a live result count and a custom Sort dropdown (Featured, A to Z, Newest). | `_dev/reference/components/directory-results-toolbar.html` |
| `directory-person-card` | paste (inline CSS) | The card each person gets on /people and on people search: a 4:3 real portrait (or a Gazpacho monogram tile while a portrait is being sourced), an optional inherited paid-tier badge, name with a claimed-profile tick, role, city, expertise tags and a Connect button, in a three, two, one column grid. | `_dev/reference/components/directory-person-card.html` |

## Design pillar

| id | kind | what it is | source |
|---|---|---|---|
| `design-pillar-partner` | paste (inline CSS) | The full-width closing pitch at the foot of a vertical: icon disc, Brand Connect eyebrow scoped to the vertical, Gazpacho line, lead and CTA. The platform's own sales close, one link to the Brand Connect page. | `_dev/reference/components/design-pillar-partner.html` |
| `design-pillar-spread` | paste (inline CSS) | Six curated stories in three columns, with a taller centre column (16:9 thumbs, larger titles) flanked by square-thumb columns. The pillar's cover-story moment, chosen by editors rather than by date. | `_dev/reference/components/design-pillar-spread.html` |
| `design-pillar-brief-card` | paste (inline CSS) | A quiet split card, photograph on one side and an eyebrow, Gazpacho title, one-line lead and text CTA on the other. Sends a reader with a project to a brief flow rather than a listing scroll. | `_dev/reference/components/design-pillar-brief-card.html` |
| `design-pillar-adslot` | paste | The sellable in-flow ad unit: campaign image on the left fading into a brand-coloured panel on the right, with a Sponsored badge, brand eyebrow, Gazpacho headline, short lead and a white pill CTA. The whole unit is one link. | `_dev/reference/components/design-pillar-adslot.html` |
| `design-pillar-filter-strip` | paste (inline CSS) | An in-page row of pill chips that narrows the story stream below it by sub-topic. Scrolls sideways when it overflows and scrolls away with the stream, so it reads as a filter, not a third nav bar. | `_dev/reference/components/design-pillar-filter-strip.html` |
| `design-pillar-hero` | paste (inline CSS) | The type-only opening of a /design pillar or collection landing: one Gazpacho title and a one-line dek. No image, so the page name reads the same at any length and nothing competes with the stream below. | `_dev/reference/components/design-pillar-hero.html` |
| `design-pillar-quotes` | paste (inline CSS) | One large centred quote at a time with the speaker's name and a read link, paged with the shared prev/next paginator. Brings Industry Voices into a Subject pillar without a card grid. | `_dev/reference/components/design-pillar-quotes.html` |
| `design-pillar-series-rail` | paste (inline CSS) | A titled horizontal rail of 4:5 series posters with the name and story count under each. Drags on desktop and swipes natively on touch through the shared carousel chassis. | `_dev/reference/components/design-pillar-series-rail.html` |
| `design-sponsored-scene` | paste (inline CSS) | A cinematic 21:9 image that grows from 88% to full size as it scrolls through the viewport, with a warm-white panel fading up in its centre carrying the title, the collaboration disclosure and a Read story button. The treatment reserved for partnered and paid editorial. | `_dev/reference/components/design-sponsored-scene.html` |
| `design-pillar-showcase` | paste (inline CSS) | One wide 21:9 overlay card with a Featured Project disclosure strip across the top and the sponsor named on the right. Sellable developer placement that still reads as a story, with the dek revealed on hover. | `_dev/reference/components/design-pillar-showcase.html` |
| `design-pillar-river` | paste (inline CSS) | A long list of horizontal story rows (4:3 thumb left, eyebrow, Gazpacho title and byline right) that shows three and reveals three more per click. Rows can carry a Sponsored pill, and a native billboard can sit in the list as one more item. | `_dev/reference/components/design-pillar-river.html` |
| `design-pillar-strip` | paste (inline CSS) | A section heading with its see-all button and three editorial cards beneath. The repeatable news-cycle unit of a pillar page: one strip per sub-topic, latest three stories each. | `_dev/reference/components/design-pillar-strip.html` |

## Industry Voices

| id | kind | what it is | source |
|---|---|---|---|
| `voices-contribute` | paste | The supply-side block: a Gazpacho title and dek on the left, two intake doors on the right (write a perspective, nominate a voice), each an icon, a label with one line of terms, and an arrow. | `_dev/reference/components/voices-contribute.html` |
| `voices-quote` | paste | A text-led card for a quote captured from elsewhere: quote mark, the verbatim sentence, one line of editorial framing, a small speaker row and the source receipt. On the landing the cards run in a free-scroll rail on the shared carousel chassis with paginator arrows. | `_dev/reference/components/voices-quote.html` |
| `voices-speaker-index` | paste | The person-first card for the Speakers index: a linked speaker head, their current claim, a list of their appearances across the portal (each with a kind label) and a View profile link. Also documents the .vx-speaker credit in its three sizes. | `_dev/reference/components/voices-speaker-index.html` |
| `voices-card` | paste | The claim-led card for our own conversations and perspectives: eyebrow, the verbatim claim in Gazpacho, a hairline, the speaker credit and the format with read time. No card media. Sits in .vx-grid (landing) or .vx-grid--archive (type pages). | `_dev/reference/components/voices-card.html` |
| `voices-hero` | paste | The full-viewport Voices landing hero. A static page intro (h1 and one-line description) above a white featured-piece card: centred kicker, a warm stage holding the ink quote box and the speaker, and a foot row with the source receipt left and the read link right. | `_dev/reference/components/voices-hero.html` |
| `voices-series` | paste | A type-only grid of series tiles: series name in Gazpacho, a one-sentence dek and a Browse cue. The in-page browse block for the Voices franchises. | `_dev/reference/components/voices-series.html` |

## GharTalks

| id | kind | what it is | source |
|---|---|---|---|
| `ghartalks-beats` | paste | A row of solid-tone tiles, one per beat, each a Gazpacho beat name and an arrow disc. The tone and ink are set per tile with --gtk-beat-tone and --gtk-beat-ink from the theme palette. | `_dev/reference/components/ghartalks-beats.html` |
| `ghartalks-card` | paste | The video-led episode card: a 16:9 still with play glyph and duration chip, then beat eyebrow, Gazpacho title, guest credit and meta. The whole card is one anchor to the episode page. Sits in .gtk-grid on beat and search pages, with .gtk-card--lead for a spanning first card and .gtk-empty for zero results. | `_dev/reference/components/ghartalks-card.html` |
| `ghartalks-hero` | paste | The GharTalks landing hero: a static page intro (h1 and one-line description) above the featured episode, a single link joining a 16:9 video still with a Featured chip and play glyph to a panel with kicker, episode title, dek and guest credit. | `_dev/reference/components/ghartalks-hero.html` |
| `ghartalks-rail` | paste | A landing strip: title, optional dek and arrow pair in .gtk-strip__head, over a .gtk-rail on the shared carousel chassis. The rail pads all four sides so card shadows are not clipped. Carries episode cards or portrait guest cards; the arrows hide themselves when the track does not overflow. | `_dev/reference/components/ghartalks-rail.html` |
| `ghartalks-format` | paste | A two-column feature that explains the format by showing one episode: a linked 16:9 still on one side, and on the other an eyebrow, a Gazpacho title, an excerpt from the written-up episode and a Read the write-up link. | `_dev/reference/components/ghartalks-format.html` |

## Articles

| id | kind | what it is | source |
|---|---|---|---|
| `article-body` | paste (inline CSS) | The reading column and its prose blocks: lead paragraph with drop cap, h2 and h3, lists, marks and footnote refs, a specs list, inline images (wide and portrait) that open in the photo lightbox, the pull quote (plain and with a byline head), the long blockquote, a section divider and footnotes. | `_dev/reference/components/article-body.html` |
| `article-hero` | paste (inline CSS) | The top of a story page: a thin reading-progress bar, then eyebrow, Gazpacho title, deck and the byline (avatar and author link, updated tag with a link to the editor's note, date, read time), then a full-width hero figure with caption and credit. | `_dev/reference/components/article-hero.html` |
| `article-notes` | paste (inline CSS) | Trust blocks inside a story body: the editor's note (default), its disclosure, warning and correction variants, each a left rule with a label, and the methodology and sources panel as a definition list. | `_dev/reference/components/article-notes.html` |
| `article-callout` | paste (inline CSS) | A bordered link row inside a story: initials tile or face, a small label, the name and a sub line, and an arrow. One chassis for a brand, a person, a project or a read-next story; with .art-speaker-strip and a face it becomes the subject credit under the hero of a Conversation. | `_dev/reference/components/article-callout.html` |
| `article-slots` | paste (inline CSS) | Server-filled placement slots inside a story body. .art-slot carries the slot type, id, size and audience as data attributes; inside it sit the placement faces: a sponsor profile card (.pl-brand), a display ad (.pl-ad) and a newsletter prompt (.pl-news). | `_dev/reference/components/article-slots.html` |
| `article-qa` | paste (inline CSS) | Question and answer pairs for an interview piece. Each question and answer opens with a small initials tag for who is speaking; answers set heavier than questions. | `_dev/reference/components/article-qa.html` |
| `article-related` | paste (inline CSS) | The discovery sections after a story, outside the body column: .art-section with a compact section head, holding either a three-up .art-card-grid of image cards (More from the series, Related) or an .art-across-list of type-only cards pointing to the same subject in other verticals. | `_dev/reference/components/article-related.html` |
| `article-media` | paste (inline CSS) | Media blocks inside a story body: a horizontal scroll-snap photo carousel whose frames open in the lightbox, an audio episode card linking to a GharTalks episode, and a static map snapshot with a pin and caption. | `_dev/reference/components/article-media.html` |
| `article-summary` | paste (inline CSS) | The orientation blocks at the top of a story body: the series strip (Part n of m in Series name), the short-version summary, the collapsible In this story contents list built from the body's h2s, and a key takeaways bullet list. | `_dev/reference/components/article-summary.html` |
| `article-disclosure` | paste (inline CSS) | A one-line note between the article hero and the body on a paid story: brand initial mark, Presented by and the brand name, a divider, and the Paid Partnership tag. | `_dev/reference/components/article-disclosure.html` |
| `article-data` | paste (inline CSS) | Data blocks inside a story body: the single big stat with caption, the stat strip of two to four figures, the two-column comparison with a vs divider, and the article table in its variants (zebra, cards on mobile, minimal, bordered, row-header). | `_dev/reference/components/article-data.html` |
| `article-end` | paste (inline CSS) | The end matter at the foot of a story body: the Tagged row of tag pills, the credits grid (words, reporting, photography, editing, in this story), and the Continue the series panel with part progress and previous and next story links. | `_dev/reference/components/article-end.html` |

## B2B pages

| id | kind | what it is | source |
|---|---|---|---|
| `b2b-contact-cta` | paste (inline CSS) | The last band of a B2B page: a dark rounded card with warm glows and a G watermark, holding a small label, a white Gazpacho headline, one line, the red CTA and a row of call and email chips. | `_dev/reference/components/b2b-contact-cta.html` |
| `b2b-market` | paste (inline CSS) | A two-part white card: on the left one sourced sector figure in Gazpacho with a trend pill, what it means and a source line; on the right a warm panel cut with a chevron edge, listing who the brand reaches with icon tiles over faint concentric rings. | `_dev/reference/components/b2b-market.html` |
| `b2b-packages` | paste (inline CSS) | Three tier cards side by side: a tinted top with badge, name, one-line promise and who it is for, a checklist body where each tier inherits the one before, and a CTA; the middle tier is featured with a deeper shadow and a dark CTA, and moves first when the grid stacks. | `_dev/reference/components/b2b-packages.html` |
| `b2b-section` | paste (inline CSS) | The base every modern B2B page (Brand Connect, For Brands, Design partner kit) builds on: page tokens on :root, a full-bleed .band with a .band--warm alternate, a 1140px .wrap, the numbered .eyebrow, a Gazpacho h2, .lead and .sub copy, and the red .btn with a white .btn-ghost. | `_dev/reference/components/b2b-section.html` |
| `b2b-hero` | paste (inline CSS) | The opening band of a B2B sales page: Ghar.tv wordmark with a product tag, a two-line Gazpacho headline that can carry masked photo glyphs inline, a lede, the red and ghost CTA pair, and a Built for row of audience chips, over a faint G watermark. | `_dev/reference/components/b2b-hero.html` |
| `b2b-alacarte` | paste (inline CSS) | The a la carte block under the packages: a label, a small headline and sub line, then two columns of colour-keyed service groups, each a dot-and-name head over rows of a bold service name and what it does, closed by a note and a centred CTA. | `_dev/reference/components/b2b-alacarte.html` |
| `b2b-surface-map` | paste (inline CSS) | A dark band that maps every place a brand can appear: up to five colour-keyed pillar columns of translucent channel cards, a two-up wrapping row for cross-cutting services, and a closing note, over a diagonal hatch, soft glows and a G watermark. | `_dev/reference/components/b2b-surface-map.html` |
| `b2b-why` | paste (inline CSS) | A section head (eyebrow, headline, lead) over three white benefit cards, each with a solid theme-colour icon tile, a Gazpacho title and a short description. The tiles rotate terracotta, turmeric and sage by position. | `_dev/reference/components/b2b-why.html` |
| `b2b-legacy-compare` | paste (inline CSS) | A horizontally scrollable comparison table on the navy legacy B2B pages: a capability column, two alternative columns and a highlighted Ghar.tv column, with green ticks, faint crosses or short text in each cell. | `_dev/reference/components/b2b-legacy-compare.html` |
| `b2b-legacy-faq` | paste (inline CSS) | A single-open FAQ list on the navy legacy B2B pages: Playfair question buttons with a square plus icon that turns red and rotates into a cross when open, and answers that expand by max-height. | `_dev/reference/components/b2b-legacy-faq.html` |
| `b2b-legacy-testimonials` | paste (inline CSS) | Three quote cards on the navy legacy B2B pages: a faint oversized Playfair quote mark, the quote in italic, then the speaker name and a small mono role line, over a red top rule. Stacks to one column below 820px. | `_dev/reference/components/b2b-legacy-testimonials.html` |

## Dark vertical

| id | kind | what it is | source |
|---|---|---|---|
| `videoworks-brief` | paste | The closing section of the dark vertical: a centred warm-lit call to action with a turmeric button and a list of what the client gets back, which opens a two-step brief in the shared .jm-modal shell with a progress bar, a validated first step and a Back and Send row. | `_dev/reference/components/videoworks-brief.html` |
| `videoworks-faq` | paste | Native <details> questions on a raised dark panel: a Gazpacho question with a plus sign that turns to a minus when open, and a muted answer. No script. | `_dev/reference/components/videoworks-faq.html` |
| `videoworks-section` | paste | The frame every section of the dark vertical sits in: .vw-sec on the near-black ground with a static colour bloom in one corner, a numbered mono eyebrow, a Gazpacho headline and a lead that go two-column from 1024px, and a raised variant whose inner panel carries a faint hairline grid and bleeds to a band below 1024px. | `_dev/reference/components/videoworks-section.html` |
| `videoworks-spec` | paste | A ruled list of named deliverables on the dark ground: each row a name, a one-line description and a mono meta column for length, ratio or format, collapsing to a stacked row on phones. | `_dev/reference/components/videoworks-spec.html` |
| `videoworks-why` | paste | Four numbered reasons in a two-column grid on the dark ground: a mono key, a Gazpacho claim and a short supporting paragraph, separated by hairlines rather than cards. | `_dev/reference/components/videoworks-why.html` |
| `videoworks-scroll-hero` | paste | A 300vh pinned hero: a framed 16:9 film stage at the centre with four capability labels in the corners; scrolling scales the stage to cover the viewport while the labels leave, a sibling dims the footage, and a statement headline fills word by word. One scrubbed GSAP timeline, transform and opacity only. | `_dev/reference/components/videoworks-scroll-hero.html` |
| `videoworks-loop` | paste | A six-step process grid with hairline gaps inside a raised panel: each step a mono number, a Gazpacho verb and one line, closed by a full-width row with a cycle glyph saying how the end feeds the start. The torch glow washes the cells. | `_dev/reference/components/videoworks-loop.html` |
| `videoworks-services` | paste | A grid of six dark service cards, each lit from one corner by its own theme tone (the CRED card move), with a mono number, a canonical house silhouette with a service glyph knocked out of it, a Gazpacho title, a description and a deliverables list. Under a fine pointer a torch glow follows the cursor across the whole grid. | `_dev/reference/components/videoworks-services.html` |
| `videoworks-links` | paste | A row of dark link cards to the sibling products a finished piece of work runs on, each lit by its own tone with an arrow that moves on hover, sharing the torch glow of the service cards. | `_dev/reference/components/videoworks-links.html` |

